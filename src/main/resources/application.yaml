spring:
  application:
    name: spring-paging-sorting
  datasource:
    url: jdbc:mysql://localhost:3306/nguyethaDB
    username: jason
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
server:
  port: 8080
